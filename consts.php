<?php // phpcs:ignore WordPress.Files.FileName.InvalidClassFileName

if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly.
}

define( 'JCORE_UTILS_BLOCKS_URL', plugin_dir_url( __FILE__ ) );
define( 'JCORE_UTILS_BLOCKS_PATH', plugin_dir_path( __FILE__ ) );
